# Example env for backend
PORT=4000
# Postgres connection string (postgres://user:pass@host:port/dbname)
DATABASE_URL=postgres://postgres:password@localhost:5432/billing
JWT_SECRET=replace-this-with-a-secure-secret # REQUIRED: set a strong random secret in production
# Seed admin credentials (used by scripts/seedAuth.js)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=admin123
# Use secure cookies when true (requires HTTPS). MUST be true in production.
COOKIE_SECURE=false
# Optional: paths to TLS cert and key to start HTTPS locally (PEM files)
#SSL_CERT_PATH=path\to\cert.pem
#SSL_KEY_PATH=path\to\key.pem
